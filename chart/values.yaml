# Default values for prowd.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: quay.io/deis/prowd
  tag: canary
  pullPolicy: Always
debug: false
